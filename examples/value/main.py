

if __name__ == "__main__":
    a = self.outgoing[word]
    self.outgoing[word] += "b"
    key = heaviest[r.randint(0, len(heaviest)-1)]
    self.wordlist[w].incrementCount()
    s = sorted(self.wordlist.keys(), key=lambda word: self.wordlist[word].count)
