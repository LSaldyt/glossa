import std
import dog

main():
    print("Running Main")

    dogs = [Dog(), Dog()]
    for dog in dogs:
        print(dog.sound())
    end

    dog_tuple = <Dog(), Dog()>

    for n in [10, 5]:
        i = n
        while i > 0:
            i = i - 1
            if i % 2 == 0:
                print("EVEN")
            else:
                print("ODD")
            end
            print(i)
        end
    end

    return 0
